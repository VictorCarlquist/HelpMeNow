@startuml
	state "Consultando chamados" as consulta
	state "Selecionando chamado" as seleciona
	state "Fechando chamado" as fecha

	state ifopt <<choice>>


	[*] -> consulta
	consulta : + do  / conChamados()
	consulta -> seleciona
	seleciona : + do / getChamado(index:int)
	seleciona --> ifopt : Fechar chamado ?
	ifopt -> fecha : Sim
	fecha : + do / closeChamado()

	fecha -->[*]
	ifopt --> [*] : Não

@enduml